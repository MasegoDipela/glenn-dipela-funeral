# Portrait images

Place the actual portrait images here, replacing the placeholders:

- `malome-glenn-primary.jpg` — main hero portrait (recommended 1200×1500, 4:5 ratio).
- `malome-glenn-secondary.jpg` — optional secondary photograph.

Until real images are added, the site will show a graceful placeholder.
For best results use a high-resolution, well-lit colour portrait.
